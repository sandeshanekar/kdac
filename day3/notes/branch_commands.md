# git branching

```bash

# get the list of existing branches
> git branch

# create a new branch
> git branch <branch name>

# checkout a branch
> git checkout <branch name>

# merge the changes of one branch to another
# the changes from said branch will be merged to the current branch
> git merge <branch name>

```
